<h1>Lists</h1>
<%# <% @lists.each do |list|%>

<% @lists.each do |list| %>
  <div class = "card border-succes mb-3">
  <p class="card-title"><%= list.name %></p>
  <%= link_to "See bookmarks", list_path(list.id), class: "btn btn-outline-primary" %>
  <%# <%= link_to "", restaurant_show_path(restaurant), class: "btn btn-outline-primary"%>
  </div>
<% end %>

<%= link_to "New list", new_list_path, class: "btn btn-outline-primary" %>

<%= render "form" %>
